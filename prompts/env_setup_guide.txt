请基于提供的文件内容，为我提供详细的PHPStudy测试环境搭建指导。

**重要要求：请使用中文回复，所有输出内容必须是中文。**

**重要说明：这是用于代码审计的测试环境，不需要考虑生产环境的安全性要求。**

## 项目信息
- 项目文件夹名称: {project_folder_name}
- 检测到的文件类型: {file_types}
- 访问地址: http://cvehunter.test/{project_folder_name}

## PHPStudy测试环境搭建详细步骤

### 第一步：文件部署
1. **确保项目文件夹放置到PHPStudy的WWW目录下**
   - PHPStudy默认WWW目录通常位于: `PHPStudy安装目录/WWW/`
   - 将整个项目文件夹 `{project_folder_name}` 复制到WWW目录下
   - 最终路径应为: `PHPStudy/WWW/{project_folder_name}/`

### 第二步：启动PHPStudy服务
1. **手动打开PHPStudy控制面板**
2. **启动WNMP服务**
   - 点击"启动"按钮启动Apache/Nginx服务
   - 点击"启动"按钮启动MySQL数据库服务
   - 确保两个服务都显示为"已启动"状态

### 第三步：数据库配置（如果项目需要数据库）
1. **创建测试数据库**
   - 在PHPStudy控制面板中点击"数据库"按钮
   - 选择"创建数据库"
   - 填入数据库名称（根据项目配置文件确定）
   - 字符集建议选择 utf8mb4_general_ci
   - 点击"确认"创建数据库

2. **创建独立的测试数据库用户**
   - **重要**: 必须创建一个新的数据库用户（不要使用root或其他已有用户）
   - **目的**: 便于后续快速清除整个测试数据库而不影响其他数据
   - 在数据库管理界面中选择"用户管理"
   - 点击"添加用户"
   - 填入用户名（建议格式: `test_项目名` 或 `audit_项目名`）
   - 填入密码（与数据库连接文件中的密码保持一致）
   - 为该用户分配对应测试数据库的完整权限（SELECT, INSERT, UPDATE, DELETE, CREATE, DROP等）
   - 点击"确认"创建用户

3. **修改项目数据库配置文件**
   - 找到项目中的数据库连接配置文件
   - 将数据库主机改为: localhost 或 127.0.0.1
   - **必须修改**: 将数据库用户名和密码改为上一步创建的独立测试用户信息
   - 确保数据库名称与创建的测试数据库一致

4. **导入SQL文件（重要步骤，不可省略）**
   - **必须执行**: 如果项目包含.sql文件，必须通过以下步骤导入：
   
   - **⚠️ 重要提醒：查找.sql文件位置**
     - .sql数据库文件通常位于以下目录中：
       * `database/` 文件夹
       * `sql/` 文件夹  
       * `db/` 文件夹
       * `data/` 文件夹
       * `install/` 文件夹
       * `setup/` 文件夹
       * 项目根目录
     - 常见的.sql文件名：
       * `database.sql`
       * `install.sql`
       * `setup.sql`
       * `init.sql`
       * `项目名.sql`
     - **请仔细检查项目文件夹，确保找到所有.sql文件**
   
   - **导入步骤**：
     - 在PHPStudy控制面板中点击"数据库"按钮
     - 选择刚创建的测试数据库
     - 点击"操作"菜单
     - 选择"导入"功能
     - 点击"浏览"按钮，选择项目预置的.sql文件
     - 点击"确认"开始导入
     - **如果有多个.sql文件，请按照install.sql → database.sql → data.sql的顺序导入**
   
   - **重要性**: 这一步是为了后续能够单独快速清除这个测试数据库

### 第四步：项目特定配置
请根据以下文件内容分析并提供具体的配置指导：

**请特别关注以下方面：**
1. **配置文件修改**: 分析配置文件中需要修改的参数（使用相对路径）
2. **权限设置**: 指出哪些文件夹需要写入权限
3. **⭐ 系统内置测试账户**: **必须详细列出**项目中所有内置的默认登录凭据，包括：
   - 管理员账户（用户名/邮箱和密码）
   - 普通用户账户（用户名/邮箱和密码）
   - 其他角色账户（如果存在）
   - 数据库中预设的测试账户信息

**输出格式要求：**
- **重点突出**：系统内置测试账户部分必须详细完整
- 提供详细的配置文件修改步骤（文件路径使用相对路径格式）
- 给出完整的测试访问流程

**输出内容限制：**
- 没必要进行项目结构预览
- 没必要常见问题及解决
- 没必要PHP扩展检查
- 没必要告诉用户验证文件完整性

**测试环境说明：**
- 这是代码审计测试环境，保持原有的默认配置和账号密码
- 重点是确保环境能够正常运行以便进行漏洞测试
- 独立的数据库用户设置是为了便于后续清理测试数据

由于我已经在hosts文件中配置了 `127.0.0.1 cvehunter.test`，请确保最终能通过 http://cvehunter.test/{project_folder_name} 正常访问系统。